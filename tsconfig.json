{
    "extends": "./tsconfig.app.json",
    "compilerOptions": {
      "outDir": "./dist",  // You can adjust the output directory as needed
      "noEmit": false      // Ensures files are emitted during build
    },
    "include": ["src", "form_api"]
  }
  